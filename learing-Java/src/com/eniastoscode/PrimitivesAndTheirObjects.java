package com.eniastoscode;
import java.util.List;
public class PrimitivesAndTheirObjects {
    public static void main(String[] args) {
        //Integer number = 1;
        List<Integer> numbers = null;
        numbers.add(1);
        //int number = null; does not work but
        Integer number = null;// works
        //numbers.add("1"); this string will not work.
        // double >> Double
        // long >> Long, int >> Integer
        // For every single primitive there is an object version
    }
}

// For very single primitive , we have the object equivalent.
// Very trivial in data structures and collections
